
import random

messages = ['It is certain',
            'It is decidedly so',
            'Yes definitely',
            'Reply hazy try again',
            'Ask again later']

print(messages[random.randint(0, len(messages) - 1)])